var expect = require('expect');

describe('App', () => {
  it('should properly run tests', () => {
    expect(1).toBe(1);
  });
});

// var expect = require('expect');

// 	describe('App', () => {
// it('should properly run the tests', () => {
// 	expect(1).toBe(1);
// })
// });
